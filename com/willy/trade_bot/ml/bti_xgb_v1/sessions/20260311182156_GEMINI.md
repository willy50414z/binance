# Review Report & Updated Plan - 20260311182156

## 1. 執行摘要 (Execution Summary)
- **原始報告**: `20260311182006_GEMINI.md`
- **核心問題**: 零交易信號 (Signal Starvation)、過早停止 (Early Stopping at Iteration 4)。
- **專家評語**: 目前模型處於「不學習」狀態，主因可能是驗證集切分過細導致統計雜訊過大，以及特徵/標籤組合在當前視界下無顯著相關性。

## 2. 訓練結果診斷 (Expert Diagnosis)
- **機率分佈坍塌**: 預測機率極大機率集中在 0.33 附近，代表特徵未提供足夠辨識力。
- **驗證集樣本荒**: 5-way split 在 walk-forward 窗口中導致各階段樣本極少（可能 < 500 筆），這使得 `find_best_thresholds` 難以跨過「至少 5 筆交易」的門檻。
- **Early Stopping 誤判**: 迭代 4 次即停止，通常是因為驗證集太小且分佈與訓練集不一致，導致 loss 隨機波動。

## 3. 補充建議計畫 (Supplementary Advice)
- [ ] **驗證集整合**: 嘗試將 `val_earlystop` 與 `val_cal` 合併，以增加樣本量。
- [ ] **特徵重要性監控**: 即使無信號，也要輸出 Feature Importance，檢查是否有 feature 貢獻為 0。
- [ ] **輸出 Raw Probabilities**: 在診斷報告中增加 `max_prob` 的平均值與標準差，確認模型是否「完全沒信心」。
- [ ] **標籤平衡度檢查**: 確認 Training Set 中各類別的真實比例，避免 `balanced_weight` 過度補償導致的雜訊。

## 4. 衝突觀點對照表 (Conflicting Ideas)

| 衝突項目 | 原始修改計畫 (20260311182006) | ML 專家建議觀點 | 優劣分析 |
| :--- | :--- | :--- | :--- |
| **Threshold 掃描下限** | 嘗試從 **0.35** 開始。 | 建議最低維持在 **0.40**。 | **0.35**: 易產生信號但雜訊極高，接近隨機猜測。<br>**0.40**: 維持起碼的預測信心門檻，避免過度交易。 |
| **Calibration 選擇** | 比較 Isotonic 與 Sigmoid。 | 傾向 **Sigmoid** 或 **Raw**。 | **Isotonic**: 在小樣本下極易 Overfit 驗證集的機率分佈。<br>**Sigmoid**: 參數化模型，對小樣本較為 Robust。 |
| **標籤優化路徑** | 降低 ATR Multiplier 至 **1.0**。 | 優先增加 **Lookahead** (視界)。 | **降低 ATR**: 增加標籤數量，但可能引入更多隨機波動。<br>**增加 Lookahead**: 尋找更長線的穩定趨勢，減少短線雜訊干擾。 |

## 5. 最終行動清單 (Final Action Plan)
1.  **[優先]** 修改 `model_trainer.py` 輸出機率分佈直方圖與特徵重要性。
2.  **[診斷]** 檢查 `val_th` 階段產生的 candidate 組合數量，若為 0，則放寬 `total_trades >= 5` 的限制至 `total_trades >= 1` 進行觀察。
3.  **[實驗]** 執行 `lookahead=8` 與 `lookahead=12` 的掃描，觀察是否因視界過短導致模型無法學習。
4.  **[調整]** 若上述無效，再執行 `learning_rate=0.01` 並合併驗證集。

---
*Reviewed by Gemini CLI (ML Expert Mode)*
